{
  "name": "pointjoy",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev -w apps/api\" \"npm run dev:h5 -w apps/mini\"",
    "db:up": "docker compose up -d",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api",
    "build": "npm run build -w apps/api && npm run build:h5 -w apps/mini && npm run build:mp-weixin -w apps/mini",
    "test": "node --test tests/*.test.mjs",
    "setup": "node scripts/setup.mjs",
    "check": "npm run build && npm run type-check -w apps/mini && npm test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "overrides": {
    "vite": "5.4.21"
  }
}