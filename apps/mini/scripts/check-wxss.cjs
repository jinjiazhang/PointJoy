// This catches known WXSS-incompatible selectors; WeChat CI remains the final compiler.
const fs=require('node:fs'),path=require('node:path'),postcss=require('postcss');
const root=path.resolve(__dirname,'../dist/build/mp-weixin');let count=0,rules=0;const errors=[];
function inspect(dir){for(const file of fs.readdirSync(dir,{withFileTypes:true})){const name=path.join(dir,file.name);if(file.isDirectory()){inspect(name);continue}if(!name.endsWith('.wxss'))continue;count++;const css=postcss.parse(fs.readFileSync(name,'utf8'),{from:name});css.walkRules(rule=>{rules++;const selector=rule.selector;const withoutAttributes=selector.replace(/\[[^\]]+\]/g,'');if(/\*/.test(withoutAttributes))errors.push(name+': unsupported universal selector '+selector);if(/:(?:is|where|has|not|nth-child|nth-of-type|first-child|last-child)\b/.test(selector))errors.push(name+': unsupported functional or structural selector '+selector);if(/[>+~]/.test(withoutAttributes))errors.push(name+': avoid complex selector '+selector);if(/\[/.test(selector)&&selector!=='[data-c-h="true"]')errors.push(name+': application attribute selector must use explicit class '+selector);});}}
if(!fs.existsSync(root))throw new Error('Build the WeChat target first');inspect(root);if(!count)throw new Error('No WXSS build outputs found');if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`WXSS compatibility scan passed: ${count} file(s), ${rules} rules. Final validation still requires WeChat CI.`);
